import React, { useState } from 'react'

const UseState = () => {
    const[count,setCount]= useState(0);
    const addFive=()=>{
        for(const)
    }
  return (
    <div>

    </div>
  )
}

export default UseState